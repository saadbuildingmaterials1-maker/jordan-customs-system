# دليل تحديث DNS والنشر على mp3-app.com
# DNS Update and Deployment Guide

**التاريخ**: 11 فبراير 2026  
**الحالة**: جاهز للنشر الفوري

---

## 🎯 معلومات النطاق المخصص

| المعلومة | القيمة |
|---------|--------|
| **النطاق** | `mp3-app.com` |
| **الخادم المؤقت** | `3000-iez20yhz3jlh0ce65dmdy-c99488f0.sg1.manus.computer` |
| **النوع** | CNAME أو A Record |
| **SSL** | Let's Encrypt (تجديد تلقائي) |
| **المنطقة الجغرافية** | سنغافورة (SG1) |

---

## 📋 خطوات تحديث DNS

### الخطوة 1: الوصول إلى موفر النطاق

1. اذهب إلى موقع موفر النطاق (GoDaddy, Namecheap, Cloudflare, إلخ)
2. قم بتسجيل الدخول إلى حسابك
3. ابحث عن قسم "DNS Management" أو "Domain Settings"

### الخطوة 2: إضافة سجل CNAME

**الطريقة الأولى (موصى بها)**:

```
النوع: CNAME
الاسم: mp3-app.com (أو @ إذا كان الخيار الأول غير متاح)
القيمة: 3000-iez20yhz3jlh0ce65dmdy-c99488f0.sg1.manus.computer
TTL: 3600 (أو القيمة الافتراضية)
```

**الطريقة الثانية (إذا لم تعمل الطريقة الأولى)**:

```
النوع: A Record
الاسم: mp3-app.com (أو @)
القيمة: 34.159.200.1
TTL: 3600 (أو القيمة الافتراضية)
```

### الخطوة 3: حفظ التغييرات

1. انقر على "Save" أو "Update"
2. قد تظهر رسالة تأكيد
3. انتظر حتى يتم حفظ التغييرات

---

## ⏱️ انتظار انتشار DNS

**المدة المتوقعة**: 24-48 ساعة

**ملاحظات**:
- قد ينتشر في بعض المناطق في دقائق قليلة
- قد يستغرق وقتاً أطول في مناطق أخرى
- استخدم أداة التحقق للتأكد من الانتشار

---

## ✅ التحقق من انتشار DNS

### الطريقة 1: استخدام موقع DNS Checker

1. اذهب إلى: https://dnschecker.org
2. أدخل: `mp3-app.com`
3. اختر: `CNAME` أو `A Record`
4. انقر: "Check"
5. انتظر النتائج من جميع الخوادم

### الطريقة 2: استخدام سطر الأوامر (Windows)

```cmd
nslookup mp3-app.com
```

### الطريقة 3: استخدام سطر الأوامر (Mac/Linux)

```bash
dig mp3-app.com
# أو
nslookup mp3-app.com
```

### النتيجة المتوقعة

```
mp3-app.com CNAME 3000-iez20yhz3jlh0ce65dmdy-c99488f0.sg1.manus.computer
```

---

## 🔒 التحقق من SSL

بعد انتشار DNS، سيتم إنشاء شهادة SSL تلقائياً:

1. اذهب إلى: `https://mp3-app.com`
2. تحقق من وجود قفل أخضر في المتصفح
3. انقر على القفل للتحقق من تفاصيل الشهادة

---

## 🚀 النشر على Manus

### الخطوة 1: تحديث إعدادات المشروع

```bash
# في لوحة التحكم Manus:
1. انتقل إلى: Settings → Domains
2. أضف النطاق الجديد: mp3-app.com
3. اختر: Custom Domain
4. أدخل: mp3-app.com
5. انقر: Verify
```

### الخطوة 2: تفعيل النطاق

```bash
# بعد التحقق:
1. انقر: Activate
2. انتظر التأكيد
3. سيتم إعادة توجيه جميع الطلبات إلى mp3-app.com
```

### الخطوة 3: اختبار النطاق

```bash
# اختبر الرابط الجديد:
https://mp3-app.com

# تحقق من:
- الصفحة الرئيسية تحميل بنجاح
- جميع الأزرار تعمل
- الروابط الداخلية صحيحة
- SSL يعمل (قفل أخضر)
```

---

## 📊 جدول الحالة

| المرحلة | الحالة | الوقت |
|--------|--------|-------|
| **تحديث DNS** | ⏳ جاهز | الآن |
| **انتشار DNS** | ⏳ 24-48 ساعة | بعد التحديث |
| **تفعيل SSL** | ⏳ تلقائي | بعد الانتشار |
| **تفعيل النطاق** | ⏳ جاهز | بعد الانتشار |
| **الاختبار النهائي** | ⏳ جاهز | بعد التفعيل |

---

## 🔧 استكشاف الأخطاء

### المشكلة: DNS لم ينتشر بعد 48 ساعة

**الحل**:
1. تحقق من أن السجل تم إضافته بشكل صحيح
2. تأكد من عدم وجود سجلات متضاربة
3. جرب مسح ذاكرة التخزين المؤقت للـ DNS:
   - Windows: `ipconfig /flushdns`
   - Mac: `sudo dscacheutil -flushcache`
   - Linux: `sudo systemctl restart systemd-resolved`

### المشكلة: SSL لم يتم إنشاؤه

**الحل**:
1. تأكد من انتشار DNS أولاً
2. انتظر 5-10 دقائق بعد الانتشار
3. أعد تحميل الصفحة (Ctrl+F5)

### المشكلة: الموقع لا يفتح على النطاق الجديد

**الحل**:
1. تحقق من تفعيل النطاق في Manus
2. تأكد من أن DNS ينتشر بشكل صحيح
3. جرب مسح ذاكرة التخزين المؤقت للمتصفح

---

## 📞 الدعم الفني

إذا واجهت مشاكل:

1. **تحقق من حالة DNS**: https://dnschecker.org
2. **تحقق من SSL**: https://www.ssllabs.com/ssltest/
3. **اتصل بدعم Manus**: https://help.manus.im

---

## ✨ ملاحظات مهمة

- جميع البيانات محفوظة وآمنة
- لا توجد فترة توقف أثناء النشر
- يمكنك استخدام النطاق المؤقت حتى اكتمال الانتشار
- شهادة SSL تتجدد تلقائياً كل 90 يوم

---

**آخر تحديث**: 11 فبراير 2026 - 20:30 UTC
