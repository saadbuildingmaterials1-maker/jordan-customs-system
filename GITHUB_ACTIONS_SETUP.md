# دليل إعداد GitHub Actions

## نظرة عامة

تم إعداد سير عمل تلقائي شامل باستخدام GitHub Actions لـ:
- بناء التطبيق على جميع المنصات (Windows, macOS, Linux)
- اختبار الكود والتحقق من الجودة
- نشر الإصدارات الجديدة تلقائياً
- إنشاء نسخ احتياطية تلقائية
- إرسال الإشعارات

---

## Workflows المتاحة

### 1. بناء ونشر التطبيق (build-and-release.yml)

**المحفزات:**
- عند إنشاء tag جديد بصيغة `v*` (مثل: v1.0.0)
- عند تشغيل يدوي (workflow_dispatch)

**المهام:**
- بناء التطبيق على Ubuntu, Windows, macOS
- رفع الملفات المُنتجة
- نشر الإصدار على GitHub Releases
- إرسال إشعارات البريد الإلكتروني و Slack و Discord

**الملفات المُنتجة:**
- Windows: `.exe` و `*-portable.exe`
- macOS: `.dmg`
- Linux: `.AppImage`

### 2. اختبار الكود (test.yml)

**المحفزات:**
- عند الـ push على main أو develop
- عند فتح Pull Request

**المهام:**
- اختبار على Node.js 16, 18, 20
- فحص أخطاء TypeScript
- تشغيل اختبارات Vitest
- فحص جودة الكود
- فحص الثغرات الأمنية

### 3. النشر التلقائي (auto-deploy.yml)

**المحفزات:**
- عند الـ push على main مع `[deploy]` في الرسالة
- عند التشغيل اليدوي

**المهام:**
- بناء التطبيق
- نشر على منصة Manus
- إنشاء نسخ احتياطية
- إرسال الإشعارات

---

## إعداد المتغيرات السرية (Secrets)

يجب إضافة المتغيرات التالية في إعدادات المستودع:

### 1. GitHub Token (تلقائي)
```
GITHUB_TOKEN - يتم إنشاؤه تلقائياً
```

### 2. بيانات البريد الإلكتروني
```
MAIL_SERVER     - عنوان خادم البريد (مثل: smtp.gmail.com)
MAIL_PORT       - منفذ البريد (مثل: 587)
MAIL_USERNAME   - اسم المستخدم
MAIL_PASSWORD   - كلمة المرور
```

### 3. Slack Webhook
```
SLACK_WEBHOOK   - رابط webhook من Slack
```

### 4. Discord Webhook
```
DISCORD_WEBHOOK - رابط webhook من Discord
```

### 5. قاعدة البيانات
```
DATABASE_URL    - رابط الاتصال بقاعدة البيانات
```

### 6. Snyk Token (اختياري)
```
SNYK_TOKEN      - توكن Snyk للفحص الأمني
```

---

## كيفية إضافة Secrets

### في GitHub:
1. اذهب إلى المستودع
2. Settings → Secrets and variables → Actions
3. اضغط "New repository secret"
4. أضف الاسم والقيمة

### مثال:
```
Name: MAIL_SERVER
Value: smtp.gmail.com
```

---

## كيفية استخدام Workflows

### 1. بناء ونشر إصدار جديد

```bash
# إنشاء tag جديد
git tag -a v1.0.0 -m "الإصدار 1.0.0"

# دفع الـ tag
git push origin v1.0.0
```

سيتم تلقائياً:
- بناء التطبيق على جميع المنصات
- نشر الإصدار على GitHub Releases
- إرسال الإشعارات

### 2. تشغيل الاختبارات

```bash
# الاختبارات تعمل تلقائياً عند الـ push
git push origin main
```

### 3. النشر التلقائي

```bash
# النشر مع رسالة commit تحتوي على [deploy]
git commit -m "تحديث مهم [deploy]"
git push origin main
```

أو تشغيل يدوي:
1. اذهب إلى Actions
2. اختر "نشر تلقائي"
3. اضغط "Run workflow"

---

## مراقبة الـ Workflows

### في GitHub:
1. اذهب إلى المستودع
2. اضغط على "Actions"
3. اختر الـ workflow المطلوب
4. شاهد حالة التشغيل

### الحالات الممكنة:
- ✅ **Success** - اكتمل بنجاح
- ❌ **Failure** - فشل
- ⏳ **In Progress** - قيد التنفيذ
- ⏭️ **Skipped** - تم تخطيه

---

## استكشاف الأخطاء

### المشكلة: فشل البناء على Windows
**الحل:**
1. تحقق من أن جميع المكتبات مثبتة
2. تحقق من أن `electron-builder` مثبت بشكل صحيح
3. جرب البناء محلياً أولاً

### المشكلة: الإشعارات لا تُرسل
**الحل:**
1. تحقق من صحة Secrets
2. تحقق من أن البريد الإلكتروني صحيح
3. تحقق من أن Slack/Discord webhooks صحيحة

### المشكلة: الاختبارات تفشل
**الحل:**
1. شاهد السجلات التفصيلية
2. جرب الاختبارات محلياً
3. تحقق من المكتبات المثبتة

---

## الملفات المهمة

```
.github/
├── workflows/
│   ├── build-and-release.yml    # بناء ونشر
│   ├── test.yml                 # اختبار الكود
│   └── auto-deploy.yml          # نشر تلقائي
```

---

## المراجع

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [electron-builder](https://www.electron.build/)
- [Slack Webhooks](https://api.slack.com/messaging/webhooks)
- [Discord Webhooks](https://discord.com/developers/docs/resources/webhook)

---

## الدعم

للمساعدة:
- 📧 البريد: support@manus.im
- 🌐 الموقع: www.manus.im
- 📱 الهاتف: +1-XXX-XXX-XXXX

---

**آخر تحديث:** 22 يناير 2026
**الإصدار:** 1.0.0
