# دليل التثبيت

## نظام Windows

### الطريقة 1: معالج التثبيت (الموصى به)

1. **تحميل الملف**
   - انقر على رابط التحميل المباشر
   - سيتم تحميل ملف `app-setup.exe` مباشرة

2. **تشغيل المعالج**
   - انقر نقراً مزدوجاً على الملف المحمل
   - سيظهر معالج التثبيت

3. **خطوات التثبيت**
   - اختر مجلد التثبيت (افتراضي: `C:\Program Files\...`)
   - انقر "التالي" → "التالي" → "تثبيت"
   - انتظر انتهاء التثبيت

4. **إنهاء التثبيت**
   - انقر "إنهاء"
   - ستظهر أيقونة على سطح المكتب
   - انقر على الأيقونة لتشغيل التطبيق

### الطريقة 2: الملف المحمول (Portable)

1. تحميل ملف `app-portable.exe`
2. انقر نقراً مزدوجاً على الملف
3. التطبيق يعمل مباشرة بدون تثبيت

---

## نظام macOS

### الطريقة 1: ملف DMG (الموصى به)

1. **تحميل الملف**
   - انقر على رابط التحميل المباشر
   - سيتم تحميل ملف `app.dmg`

2. **فتح الملف**
   - انقر نقراً مزدوجاً على `app.dmg`
   - سيظهر نافذة التثبيت

3. **السحب والإفلات**
   - اسحب أيقونة التطبيق إلى مجلد `Applications`
   - انتظر انتهاء النسخ

4. **تشغيل التطبيق**
   - افتح `Applications`
   - انقر نقراً مزدوجاً على التطبيق
   - قد تظهر رسالة أمان - انقر "فتح"

### الطريقة 2: ملف ZIP

1. تحميل ملف `app.zip`
2. سيتم فك الضغط تلقائياً
3. اسحب التطبيق إلى `Applications`

---

## نظام Linux

### الطريقة 1: AppImage (الموصى به)

1. **تحميل الملف**
   - انقر على رابط التحميل المباشر
   - سيتم تحميل ملف `app.AppImage`

2. **جعل الملف قابلاً للتنفيذ**
   ```bash
   chmod +x app.AppImage
   ```

3. **تشغيل التطبيق**
   ```bash
   ./app.AppImage
   ```

### الطريقة 2: حزمة DEB

1. **تحميل الملف**
   - انقر على رابط التحميل المباشر
   - سيتم تحميل ملف `app.deb`

2. **التثبيت**
   ```bash
   sudo dpkg -i app.deb
   ```

3. **تشغيل التطبيق**
   ```bash
   jordan-customs-desktop
   ```

---

## المتطلبات النظامية

### Windows
- Windows 7 أو أحدث
- معالج: Intel Core i3 أو أعلى
- ذاكرة RAM: 2 GB على الأقل
- مساحة تخزين: 500 MB على الأقل

### macOS
- macOS 10.13 أو أحدث
- معالج: Intel أو Apple Silicon
- ذاكرة RAM: 2 GB على الأقل
- مساحة تخزين: 500 MB على الأقل

### Linux
- أي توزيعة حديثة (Ubuntu 18.04+, Fedora 30+, إلخ)
- معالج: x86_64
- ذاكرة RAM: 2 GB على الأقل
- مساحة تخزين: 500 MB على الأقل

---

## استكشاف الأخطاء

### المشكلة: "لا يمكن فتح التطبيق"

**Windows:**
- تأكد من أن الملف من مصدر موثوق
- جرب تشغيل كمسؤول (انقر بزر الماوس الأيمن → "تشغيل كمسؤول")

**macOS:**
- افتح System Preferences → Security & Privacy
- انقر على "Open Anyway" بجانب التطبيق

**Linux:**
- تأكد من أن الملف قابل للتنفيذ: `chmod +x app.AppImage`

### المشكلة: "الملف معطوب"

- حذف الملف المحمل
- أعد تحميل الملف من الرابط المباشر
- تأكد من انتهاء التحميل بنسبة 100%

### المشكلة: "خطأ في التثبيت"

- تأكد من وجود مساحة تخزين كافية
- أغلق جميع البرامج الأخرى
- جرب التثبيت في مجلد مختلف

### المشكلة: "التطبيق بطيء جداً"

- تأكد من توفر ذاكرة RAM كافية
- أغلق البرامج الأخرى
- أعد تشغيل الكمبيوتر

---

## بعد التثبيت

### الخطوات الأولى

1. **تشغيل التطبيق**
   - انقر على الأيقونة على سطح المكتب
   - أو ابحث عن التطبيق في قائمة البرامج

2. **إنشاء قاعدة بيانات**
   - عند أول تشغيل، سيتم إنشاء قاعدة بيانات محلية تلقائياً
   - لا تحتاج إلى أي إعدادات يدوية

3. **استيراد البيانات**
   - استخدم ميزة "استيراد PDF" لتحميل الملفات
   - أو استخدم "إضافة شحنة" لإدخال البيانات يدويًا

4. **إنشاء التقارير**
   - اذهب إلى "التقارير"
   - اختر الصيغة المطلوبة (CSV, JSON, HTML, TXT)
   - انقر "إنشاء تقرير"

---

## التحديثات

### فحص التحديثات

1. افتح التطبيق
2. اذهب إلى "الإعدادات" → "التحديثات"
3. انقر "فحص التحديثات"

### تثبيت التحديث

1. إذا كان هناك تحديث متاح
2. انقر "تحميل وتثبيت"
3. سيتم التحديث تلقائياً
4. قد يتطلب إعادة تشغيل

---

## الإزالة

### Windows

1. اذهب إلى "لوحة التحكم" → "البرامج والميزات"
2. ابحث عن "نظام إدارة تكاليف الشحن والجمارك الأردنية"
3. انقر "إلغاء التثبيت"
4. اتبع خطوات المعالج

### macOS

1. افتح "Finder"
2. اذهب إلى "Applications"
3. اسحب التطبيق إلى "Trash"
4. أو انقر بزر الماوس الأيمن → "Move to Trash"

### Linux

```bash
# إذا كنت تستخدم DEB
sudo dpkg -r jordan-customs-desktop

# إذا كنت تستخدم AppImage
rm ~/app.AppImage
```

---

## الدعم

إذا واجهت أي مشاكل:
- 📧 البريد: support@jordancustoms.com
- 🌐 الموقع: https://jordancustoms.com
- 📖 التوثيق: https://docs.jordancustoms.com

---

**آخر تحديث:** 2024
**الإصدار:** 1.0.0
