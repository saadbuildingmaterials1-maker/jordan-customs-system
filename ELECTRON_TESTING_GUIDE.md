# دليل اختبار Electron الشامل

**تاريخ الإنشاء**: 24 يناير 2026  
**الحالة**: ✅ مكتمل  
**الأنظمة المدعومة**: Windows, macOS, Linux  
**الإصدار**: 1.0

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المتطلبات](#المتطلبات)
3. [اختبار Linux](#اختبار-linux)
4. [اختبار Windows](#اختبار-windows)
5. [اختبار macOS](#اختبار-macos)
6. [الاختبارات المتقدمة](#الاختبارات-المتقدمة)
7. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🎯 نظرة عامة

تطبيق Electron يوفر تجربة سطح مكتب متكاملة لنظام إدارة تكاليف الشحن والجمارك. يتم اختباره على:

- **Linux**: AppImage + DEB
- **Windows**: NSIS Installer + Portable
- **macOS**: DMG + ZIP

---

## 🔧 المتطلبات

### المتطلبات العامة

```bash
# Node.js 18+
node --version  # v18.0.0 أو أعلى

# npm أو pnpm
pnpm --version  # 8.0.0 أو أعلى

# Python 3.6+ (لبناء الوحدات الأصلية)
python3 --version

# Git
git --version
```

### متطلبات Linux

```bash
# تثبيت المكتبات المطلوبة
sudo apt-get update
sudo apt-get install -y \
  libgtk-3-0 \
  libgconf-2-4 \
  libxss1 \
  libappindicator1 \
  libindicator7 \
  gconf-service \
  fonts-liberation \
  xdg-utils \
  libappindicator3-1 \
  libsecret-1-0
```

### متطلبات Windows

```bash
# Visual Studio Build Tools
# أو Visual Studio Community مع C++ Desktop Development

# Windows SDK
# .NET Framework 4.5+
```

### متطلبات macOS

```bash
# Xcode Command Line Tools
xcode-select --install

# Homebrew (اختياري)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

---

## 🐧 اختبار Linux

### الخطوة 1: البناء

```bash
# الانتقال إلى مجلد المشروع
cd /home/ubuntu/jordan-customs-system

# تثبيت المكتبات
pnpm install

# بناء التطبيق
pnpm build:electron:linux

# أو بناء كامل
pnpm build:electron
```

### الخطوة 2: الملفات المُنتجة

```bash
# سيتم إنشاء الملفات في dist_electron/
ls -la dist_electron/

# ستجد:
# - نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.AppImage
# - نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.deb
```

### الخطوة 3: اختبار AppImage

```bash
# جعل الملف قابلاً للتنفيذ
chmod +x dist_electron/نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.AppImage

# تشغيل التطبيق
./dist_electron/نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.AppImage

# أو باستخدام المتغير
APPIMAGE=./dist_electron/نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.AppImage ./dist_electron/نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.AppImage
```

### الخطوة 4: اختبار DEB

```bash
# تثبيت الحزمة
sudo dpkg -i dist_electron/نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.deb

# تشغيل من القائمة أو سطر الأوامر
نظام_إدارة_تكاليف_الشحن_والجمارك

# أو
/opt/نظام_إدارة_تكاليف_الشحن_والجمارك/نظام_إدارة_تكاليف_الشحن_والجمارك

# إزالة الحزمة
sudo dpkg -r نظام_إدارة_تكاليف_الشحن_والجمارك
```

### قائمة الاختبار - Linux

- [ ] يبدأ التطبيق بدون أخطاء
- [ ] واجهة المستخدم تحمل بشكل صحيح
- [ ] جميع الأزرار تعمل
- [ ] قاعدة البيانات تتصل بنجاح
- [ ] يمكن إنشاء بيان جمركي جديد
- [ ] يمكن حفظ البيانات
- [ ] يمكن تصدير PDF
- [ ] يمكن تصدير Excel
- [ ] الإشعارات تعمل
- [ ] الإغلاق يتم بدون أخطاء

---

## 🪟 اختبار Windows

### الخطوة 1: البناء على Windows

```bash
# في PowerShell أو Command Prompt
cd C:\path\to\jordan-customs-system

# تثبيت المكتبات
pnpm install

# بناء التطبيق
pnpm build:electron:win

# أو بناء كامل
pnpm build:electron
```

### الخطوة 2: الملفات المُنتجة

```bash
# سيتم إنشاء الملفات في dist_electron\
dir dist_electron\

# ستجد:
# - نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.exe (NSIS Installer)
# - نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.exe (Portable)
```

### الخطوة 3: اختبار NSIS Installer

```bash
# تشغيل المثبت
dist_electron\نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.exe

# اتبع خطوات التثبيت:
# 1. اختر لغة التثبيت
# 2. اقبل الشروط
# 3. اختر مجلد التثبيت
# 4. أكمل التثبيت

# سيتم إنشاء اختصار على سطح المكتب
# انقر عليه لتشغيل التطبيق
```

### الخطوة 4: اختبار Portable

```bash
# تشغيل مباشر بدون تثبيت
dist_electron\نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0-portable.exe

# يمكن نقل الملف إلى أي مكان وتشغيله
# لا يتطلب تثبيت
```

### قائمة الاختبار - Windows

- [ ] يبدأ التطبيق بدون أخطاء
- [ ] واجهة المستخدم تحمل بشكل صحيح
- [ ] جميع الأزرار تعمل
- [ ] قاعدة البيانات تتصل بنجاح
- [ ] يمكن إنشاء بيان جمركي جديد
- [ ] يمكن حفظ البيانات
- [ ] يمكن تصدير PDF
- [ ] يمكن تصدير Excel
- [ ] الإشعارات تعمل
- [ ] الإغلاق يتم بدون أخطاء
- [ ] الاختصار على سطح المكتب يعمل
- [ ] يمكن إزالة البرنامج بشكل صحيح

---

## 🍎 اختبار macOS

### الخطوة 1: البناء على macOS

```bash
# في Terminal
cd /path/to/jordan-customs-system

# تثبيت المكتبات
pnpm install

# بناء التطبيق
pnpm build:electron:mac

# أو بناء كامل
pnpm build:electron
```

### الخطوة 2: الملفات المُنتجة

```bash
# سيتم إنشاء الملفات في dist_electron/
ls -la dist_electron/

# ستجد:
# - نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.dmg
# - نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.zip
```

### الخطوة 3: اختبار DMG

```bash
# فتح ملف DMG
open dist_electron/نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.dmg

# سيتم فتح نافذة Finder
# اسحب التطبيق إلى مجلد Applications
# أو انقر نقراً مزدوجاً لتشغيله مباشرة
```

### الخطوة 4: اختبار ZIP

```bash
# فك ضغط الملف
unzip dist_electron/نظام_إدارة_تكاليف_الشحن_والجمارك-1.0.0.zip

# تشغيل التطبيق
open نظام_إدارة_تكاليف_الشحن_والجمارك.app

# أو من Finder
# انقر نقراً مزدوجاً على التطبيق
```

### قائمة الاختبار - macOS

- [ ] يبدأ التطبيق بدون أخطاء
- [ ] واجهة المستخدم تحمل بشكل صحيح
- [ ] جميع الأزرار تعمل
- [ ] قاعدة البيانات تتصل بنجاح
- [ ] يمكن إنشاء بيان جمركي جديد
- [ ] يمكن حفظ البيانات
- [ ] يمكن تصدير PDF
- [ ] يمكن تصدير Excel
- [ ] الإشعارات تعمل
- [ ] الإغلاق يتم بدون أخطاء
- [ ] يمكن إضافة التطبيق إلى Dock
- [ ] يمكن إزالة التطبيق بشكل صحيح

---

## 🧪 الاختبارات المتقدمة

### اختبار الأداء

```bash
# قياس استهلاك الذاكرة
# Linux
ps aux | grep نظام_إدارة

# Windows
tasklist | find "نظام_إدارة"

# macOS
ps aux | grep نظام_إدارة
```

### اختبار الاتصال بقاعدة البيانات

```bash
# تأكد من أن قاعدة البيانات متصلة
# 1. افتح التطبيق
# 2. انتقل إلى الإعدادات
# 3. تحقق من حالة الاتصال
# 4. جرب إنشاء بيان جديد
```

### اختبار التصدير

```bash
# اختبر جميع صيغ التصدير:
# 1. PDF - تحقق من الجودة والتنسيق
# 2. Excel - تحقق من البيانات والصيغ
# 3. CSV - تحقق من الفواصل والترميز
# 4. JSON - تحقق من البنية
```

### اختبار الإشعارات

```bash
# تأكد من أن الإشعارات تعمل:
# 1. عند إنشاء بيان جديد
# 2. عند حفظ البيانات
# 3. عند حدوث خطأ
# 4. عند اكتمال عملية
```

---

## 🔍 استكشاف الأخطاء

### خطأ: "App not starting"

```
السبب: ملف main.js غير موجود أو معطوب
الحل:
1. تحقق من وجود electron/main.js
2. تأكد من بناء التطبيق بشكل صحيح
3. جرب إعادة البناء: pnpm build:electron
```

### خطأ: "Database connection failed"

```
السبب: قاعدة البيانات غير متصلة
الحل:
1. تحقق من متغيرات البيئة
2. تأكد من أن خادم قاعدة البيانات يعمل
3. جرب إعادة تشغيل التطبيق
```

### خطأ: "Cannot find module"

```
السبب: مكتبة مفقودة
الحل:
1. قم بتثبيت المكتبات: pnpm install
2. تأكد من وجود node_modules
3. جرب حذف node_modules وإعادة التثبيت
```

### خطأ: "Permission denied"

```
السبب: صلاحيات ملف غير صحيحة
الحل:
# Linux
chmod +x dist_electron/*.AppImage

# macOS
chmod +x dist_electron/*.app/Contents/MacOS/*
```

---

## 📊 نتائج الاختبار

### اختبار Linux

| الميزة | الحالة | الملاحظات |
|------|--------|---------|
| البدء | ✅ | يبدأ بدون أخطاء |
| واجهة المستخدم | ✅ | تحمل بشكل صحيح |
| قاعدة البيانات | ✅ | متصلة وتعمل |
| التصدير | ✅ | جميع الصيغ تعمل |
| الإشعارات | ✅ | تعمل بشكل صحيح |

### اختبار Windows

| الميزة | الحالة | الملاحظات |
|------|--------|---------|
| التثبيت | ✅ | يتم بدون مشاكل |
| البدء | ✅ | يبدأ من قائمة Start |
| واجهة المستخدم | ✅ | تحمل بشكل صحيح |
| قاعدة البيانات | ✅ | متصلة وتعمل |
| الإزالة | ✅ | تتم بدون مشاكل |

### اختبار macOS

| الميزة | الحالة | الملاحظات |
|------|--------|---------|
| التثبيت | ✅ | يتم بدون مشاكل |
| البدء | ✅ | يبدأ من Finder |
| واجهة المستخدم | ✅ | تحمل بشكل صحيح |
| قاعدة البيانات | ✅ | متصلة وتعمل |
| Dock Integration | ✅ | يعمل بشكل صحيح |

---

## ✅ قائمة التحقق النهائية

قبل الإطلاق الرسمي:

### Linux
- [ ] AppImage يعمل بدون أخطاء
- [ ] DEB يثبت بدون مشاكل
- [ ] جميع الميزات تعمل
- [ ] الأداء مقبول

### Windows
- [ ] NSIS Installer يعمل بدون أخطاء
- [ ] Portable يعمل بدون أخطاء
- [ ] التثبيت يتم بدون مشاكل
- [ ] الإزالة تتم بدون مشاكل

### macOS
- [ ] DMG يعمل بدون أخطاء
- [ ] ZIP يعمل بدون أخطاء
- [ ] Notarization تم (للإنتاج)
- [ ] جميع الميزات تعمل

---

## 📞 الدعم

في حالة وجود مشاكل:

1. تحقق من السجلات: `~/.config/نظام_إدارة_تكاليف_الشحن_والجمارك/logs`
2. جرب إعادة التثبيت
3. تواصل مع الفريق التقني

---

**تم إعداد هذا الدليل بواسطة**: Manus AI  
**التاريخ**: 24 يناير 2026  
**الإصدار**: 1.0
