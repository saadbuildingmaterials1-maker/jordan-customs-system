# 🔨 دليل البناء الشامل

---

## 📋 المتطلبات الأساسية

### لجميع الأنظمة:
```bash
# Node.js v14 أو أحدث
node --version

# npm v6 أو أحدث
npm --version
```

### لـ Windows:
```
- Windows 7 أو أحدث
- Visual Studio Build Tools (اختياري)
```

### لـ macOS:
```
- macOS 10.13 أو أحدث
- Xcode Command Line Tools
xcode-select --install
```

### لـ Linux:
```
- أي توزيعة حديثة
- build-essential
sudo apt-get install build-essential
```

---

## 🚀 خطوات البناء

### الخطوة 1: تحميل المشروع

```bash
# تحميل الملف
unzip jordan-customs-desktop.zip

# الدخول للمجلد
cd jordan-customs-desktop
```

### الخطوة 2: تثبيت المكتبات

```bash
npm install
```

### الخطوة 3: بناء التطبيق

#### **لـ Windows:**
```bash
npm run build:win
```

**النتيجة:** ملف `.exe` في مجلد `dist/`

#### **لـ macOS:**
```bash
npm run build:mac
```

**النتيجة:** ملف `.dmg` في مجلد `dist/`

#### **لـ Linux:**
```bash
npm run build:linux
```

**النتيجة:** ملفات `.AppImage` و `.deb` في مجلد `dist/`

#### **لجميع الأنظمة:**
```bash
npm run build:all
```

---

## 📦 الملفات الناتجة

### Windows:
```
dist/
├── نظام إدارة تكاليف الشحن والجمارك الأردنية-Setup-1.0.0.exe
└── نظام إدارة تكاليف الشحن والجمارك الأردنية-1.0.0.exe (Portable)
```

### macOS:
```
dist/
├── نظام إدارة تكاليف الشحن والجمارك الأردنية-1.0.0.dmg
└── نظام إدارة تكاليف الشحن والجمارك الأردنية-1.0.0.zip
```

### Linux:
```
dist/
├── نظام إدارة تكاليف الشحن والجمارك الأردنية-1.0.0.AppImage
└── نظام إدارة تكاليف الشحن والجمارك الأردنية-1.0.0.deb
```

---

## 🧪 تشغيل التطبيق

### في وضع التطوير:
```bash
npm start
```

### في وضع التصحيح:
```bash
npm run dev
```

---

## 🔧 استكشاف الأخطاء

### خطأ: "wine is required"
```
السبب: بناء Windows من Linux يتطلب Wine
الحل: استخدم Windows أو macOS للبناء لـ Windows
```

### خطأ: "Cannot find module"
```bash
الحل:
npm install
npm rebuild
```

### خطأ: "Permission denied"
```bash
الحل:
chmod +x dist/*.AppImage
```

---

## 📊 معلومات الإصدار

| المعلومة | التفصيل |
|---------|---------|
| **الإصدار** | 1.0.0 |
| **اسم المشروع** | jordan-customs-desktop |
| **الترخيص** | MIT |
| **المطور** | Jordan Customs Team |
| **البريد** | support@jordancustoms.com |

---

## 🎯 الملفات المتضمنة في البناء

```
✅ main.js - محرك Electron
✅ preload.js - معالج الأمان
✅ index.html - الواجهة
✅ app.js - منطق التطبيق
✅ database.js - قاعدة البيانات
✅ server.js - الخادم المحلي
✅ pdf-handler.js - معالج PDF
✅ report-generator.js - مولد التقارير
✅ update-manager.js - مدير التحديثات
✅ node_modules - المكتبات
✅ assets - الأيقونات والموارد
```

---

## 📝 ملاحظات مهمة

1. **البناء الأول قد يستغرق وقتاً طويلاً** (30-60 دقيقة)
2. **تأكد من توفر مساحة كافية** (2 GB على الأقل)
3. **استخدم اتصال إنترنت سريع** لتحميل Electron
4. **البناء يتطلب اتصال إنترنت** لتحميل المكتبات

---

## 🎉 بعد البناء

1. ابحث عن الملف في مجلد `dist/`
2. قم بتحميل الملف على خادمك
3. شارك رابط التحميل مع المستخدمين
4. المستخدمون يمكنهم تحميل وتثبيت التطبيق مباشرة

---

## 📞 الدعم

للمساعدة والدعم:
- 📧 البريد: support@jordancustoms.com
- 🌐 الموقع: https://jordancustoms.com
- 📖 التوثيق: README.md و INSTALLATION.md

---

**استمتع ببناء التطبيق!** 🚀
