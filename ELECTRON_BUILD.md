# دليل بناء تطبيق سطح المكتب

## المتطلبات

- Node.js 16+
- pnpm
- Git

## التثبيت

```bash
# تثبيت المكتبات
pnpm install

# تثبيت المكتبات الإضافية لـ Electron
pnpm add -D electron electron-builder electron-updater electron-is-dev concurrently wait-on cross-env
```

## التطوير

### تشغيل التطبيق في وضع التطوير

```bash
# تشغيل الويب و Electron معاً
pnpm run electron:dev

# أو تشغيل كل واحد على حدة
pnpm run dev              # تشغيل خادم الويب
pnpm run electron         # تشغيل Electron (في نافذة أخرى)
```

## البناء

### بناء تطبيق سطح المكتب

```bash
# بناء بدون نشر
pnpm run electron:build

# بناء مع النشر (يتطلب GitHub token)
pnpm run electron:build:publish
```

### الملفات المُنتجة

بعد البناء، ستجد الملفات في مجلد `dist_electron`:

- **Windows:**
  - `*.exe` - ملف التثبيت
  - `*-portable.exe` - نسخة محمولة

- **macOS:**
  - `*.dmg` - ملف التثبيت
  - `*.zip` - نسخة محمولة

- **Linux:**
  - `*.AppImage` - تطبيق محمول
  - `*.deb` - حزمة Debian

## التحديثات التلقائية

### إعداد GitHub

1. إنشاء مستودع GitHub جديد
2. إنشاء GitHub Token بصلاحيات `repo`
3. تعيين المتغيرات البيئية:

```bash
export GH_TOKEN=your_github_token
export GITHUB_TOKEN=your_github_token
```

### نشر التحديثات

```bash
# بناء ونشر التحديث
pnpm run electron:build:publish
```

سيتم إنشاء إصدار جديد على GitHub مع ملفات التثبيت.

## البنية

```
electron/
├── main.ts           # العملية الرئيسية
├── preload.ts        # Preload script آمن
└── ...

electron-builder.yml # إعدادات البناء
```

## الميزات

### ✅ التحديثات التلقائية
- فحص التحديثات عند بدء التطبيق
- تنزيل وتثبيت التحديثات تلقائياً
- إشعارات للمستخدم

### ✅ القائمة
- قائمة ملف (خروج)
- قائمة تحرير (تراجع، إعادة، قص، نسخ، لصق)
- قائمة عرض (تكبير، تصغير، ملء الشاشة)
- قائمة تحديث (فحص التحديثات)
- قائمة مساعدة (حول التطبيق)

### ✅ الاختصارات
- `Ctrl+Q` - خروج
- `Ctrl+Z` - تراجع
- `Ctrl+Shift+Z` - إعادة
- `Ctrl+X` - قص
- `Ctrl+C` - نسخ
- `Ctrl+V` - لصق
- `Ctrl+Plus` - تكبير
- `Ctrl+Minus` - تصغير
- `Ctrl+0` - إعادة تعيين الحجم
- `F11` - ملء الشاشة

### ✅ التكامل مع الويب
- الوصول الآمن إلى ميزات Electron من الويب
- معالجة IPC آمنة
- Context Isolation مفعّل

## استكشاف الأخطاء

### المشكلة: التطبيق لا يبدأ
**الحل:**
1. تأكد من تثبيت جميع المكتبات: `pnpm install`
2. حاول حذف `node_modules` وإعادة التثبيت
3. تحقق من أن خادم الويب يعمل على المنفذ 5173

### المشكلة: التحديثات لا تعمل
**الحل:**
1. تأكد من وجود GitHub Token
2. تحقق من أن المستودع عام (public)
3. تأكد من أن الإصدار صحيح في `package.json`

### المشكلة: الاختصارات لا تعمل
**الحل:**
1. تأكد من أن التطبيق في المقدمة (focused)
2. جرب الاختصارات المختلفة حسب نظام التشغيل

## الملفات المهمة

- `electron/main.ts` - العملية الرئيسية
- `electron/preload.ts` - Preload script
- `electron-builder.yml` - إعدادات البناء
- `package.json` - Scripts و Dependencies

## المراجع

- [Electron Documentation](https://www.electronjs.org/docs)
- [electron-builder](https://www.electron.build/)
- [electron-updater](https://www.electron.build/auto-update)

## الدعم

للمساعدة والدعم:
- 📧 البريد: support@manus.im
- 🌐 الموقع: www.manus.im
- 📱 الهاتف: +1-XXX-XXX-XXXX
