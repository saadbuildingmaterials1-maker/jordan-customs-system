# Cloudflare Pages Redirects Configuration
# تكوين إعادة التوجيه لـ Cloudflare Pages

# CRITICAL: Do NOT apply SPA fallback to static assets
# لا تطبق SPA fallback على الملفات الثابتة

# Static assets - serve as-is, no fallback
/assets/* /assets/:splat 200!
/fonts/* /fonts/:splat 200!
/images/* /images/:splat 200!
/icons/* /icons/:splat 200!

# Static files - serve as-is
/favicon.ico /favicon.ico 200!
/robots.txt /robots.txt 200!
/sitemap.xml /sitemap.xml 200!
/manifest.json /manifest.json 200!
/sw.js /sw.js 200!

# API routes - pass through
/api/* /api/:splat 200!
/trpc/* /trpc/:splat 200!

# HTML pages - apply SPA fallback ONLY for HTML
/*.html /index.html 200

# SPA fallback - ONLY for non-file routes
# This must be LAST and ONLY match routes without file extensions
/admin /index.html 200
/dashboard /index.html 200
/settings /index.html 200
/login /index.html 200
/register /index.html 200
