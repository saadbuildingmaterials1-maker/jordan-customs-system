# ✅ ربط النطاق mp3-app.com - دليل شامل
# Domain Binding Complete Guide - mp3-app.com

**التاريخ:** 11 فبراير 2026  
**النطاق:** mp3-app.com  
**الحالة:** جاهز للربط الفوري

---

## 📋 المرحلة 1: إضافة النطاق إلى Manus Platform

### الخطوة 1: الوصول إلى لوحة إدارة النطاقات

```bash
# في Manus Platform:
1. اذهب إلى: Settings > Domains
2. اضغط على: "Add Custom Domain"
3. أدخل: mp3-app.com
4. اضغط على: "Add Domain"
```

### الخطوة 2: الحصول على معلومات DNS

بعد إضافة النطاق، ستحصل على:

**Nameservers (الطريقة الموصى بها):**
```
ns1.manus.im
ns2.manus.im
ns3.manus.im
```

**أو A Record + CNAME:**
```
A Record:
  Host: mp3-app.com
  Value: 123.45.67.89 (IP الخادم)
  TTL: 3600

CNAME Record:
  Host: www.mp3-app.com
  Value: mp3-app.com
  TTL: 3600
```

---

## 🔧 المرحلة 2: تحديث DNS في موفر النطاق

### الخطوة 1: الوصول إلى لوحة التحكم

**إذا كنت تستخدم GoDaddy:**
```
1. اذهب إلى: https://www.godaddy.com/
2. اضغط على: "Sign In"
3. اذهب إلى: "My Products"
4. ابحث عن: mp3-app.com
5. اضغط على: "Manage DNS"
```

**إذا كنت تستخدم Namecheap:**
```
1. اذهب إلى: https://www.namecheap.com/
2. اضغط على: "Sign In"
3. اذهب إلى: "Domain List"
4. ابحث عن: mp3-app.com
5. اضغط على: "Manage"
```

**إذا كنت تستخدم Cloudflare:**
```
1. اذهب إلى: https://www.cloudflare.com/
2. اضغط على: "Sign In"
3. اختر: mp3-app.com
4. اذهب إلى: "DNS"
```

### الخطوة 2: تحديث Nameservers

**الطريقة الأولى: تحديث Nameservers (الأفضل)**

```
استبدل الـ Nameservers الحالية بـ:
- ns1.manus.im
- ns2.manus.im
- ns3.manus.im

ثم اضغط على: Save/Update
```

**الطريقة الثانية: إضافة A Record و CNAME (إذا لم تتمكن من تغيير Nameservers)**

```
1. أضف A Record:
   Host: mp3-app.com
   Type: A
   Value: 123.45.67.89
   TTL: 3600

2. أضف CNAME Record:
   Host: www
   Type: CNAME
   Value: mp3-app.com
   TTL: 3600

3. اضغط على: Save
```

---

## 🔐 المرحلة 3: تفعيل SSL Certificate

### الخطوة 1: طلب SSL Certificate

```bash
# في Manus Platform:
1. اذهب إلى: Settings > Domains
2. اختر: mp3-app.com
3. اضغط على: "Enable SSL"
4. اختر: "Let's Encrypt" (مجاني)
5. اضغط على: "Generate Certificate"
```

### الخطوة 2: التحقق من الملكية

```bash
# Manus سيطلب منك التحقق من ملكية النطاق عبر:
# - TXT Record في DNS
# - أو Email Verification

# إذا اخترت TXT Record:
1. انسخ القيمة المعطاة
2. أضفها إلى DNS:
   Host: _acme-challenge.mp3-app.com
   Type: TXT
   Value: [القيمة المعطاة]
3. اضغط على: Save
4. انتظر 5-10 دقائق
5. اضغط على: "Verify" في Manus
```

### الخطوة 3: تفعيل HTTPS

```bash
# بعد التحقق:
1. اذهب إلى: Settings > Domains
2. اختر: mp3-app.com
3. فعّل: "Force HTTPS"
4. اضغط على: Save
```

---

## ✅ المرحلة 4: التحقق من الانتشار

### الخطوة 1: فحص DNS

```bash
# استخدم أحد هذه الأدوات:

# 1. Google DNS Checker
https://dns.google/

# 2. MXToolbox
https://mxtoolbox.com/

# 3. DNSChecker
https://dnschecker.org/

# أدخل: mp3-app.com
# يجب أن ترى:
✅ Nameservers محدثة
✅ A Record صحيح
✅ CNAME Record صحيح
```

### الخطوة 2: اختبار الاتصال

```bash
# اختبر في Terminal:

# 1. فحص DNS
nslookup mp3-app.com
# يجب أن ترى IP الخادم

# 2. فحص الاتصال
curl -I https://mp3-app.com
# يجب أن ترى: HTTP/2 200 OK

# 3. فحص SSL
openssl s_client -connect mp3-app.com:443
# يجب أن ترى: Verify return code: 0 (ok)
```

### الخطوة 3: اختبار في المتصفح

```bash
# افتح المتصفح وادخل:
https://mp3-app.com

# يجب أن ترى:
✅ الصفحة الرئيسية للتطبيق
✅ شهادة SSL صحيحة (قفل أخضر)
✅ بدون تحذيرات أمان
```

---

## 🌍 المرحلة 5: إعدادات متقدمة

### إعادة التوجيه (Redirect)

```bash
# إذا أردت توجيه www إلى بدون www:

# في Manus Platform:
1. اذهب إلى: Settings > Domains
2. اختر: mp3-app.com
3. فعّل: "Redirect www to non-www"
4. اضغط على: Save
```

### البريد الإلكتروني (Email)

```bash
# إذا أردت استخدام البريد الإلكتروني:

# أضف MX Records:
Priority: 10
Host: mp3-app.com
Value: mail.mp3-app.com

# أضف SPF Record:
Host: mp3-app.com
Type: TXT
Value: v=spf1 include:_spf.manus.im ~all

# أضف DKIM Record:
Host: default._domainkey.mp3-app.com
Type: TXT
Value: [DKIM Key من Manus]
```

### CDN (تسريع المحتوى)

```bash
# إذا أردت استخدام CDN:

# في Manus Platform:
1. اذهب إلى: Settings > Domains
2. اختر: mp3-app.com
3. فعّل: "Enable CDN"
4. اختر: "Cloudflare" أو "Manus CDN"
5. اضغط على: Save
```

---

## 📊 المرحلة 6: المراقبة والصيانة

### فحص صحة النطاق

```bash
# تشغيل الفحص الشامل:

#!/bin/bash

echo "=== فحص صحة النطاق mp3-app.com ==="
echo ""

# 1. فحص DNS
echo "1. فحص DNS:"
nslookup mp3-app.com | grep -E "Address|Name"
echo ""

# 2. فحص الاتصال
echo "2. فحص الاتصال:"
curl -s -o /dev/null -w "HTTP Status: %{http_code}\n" https://mp3-app.com
echo ""

# 3. فحص SSL
echo "3. فحص SSL:"
echo | openssl s_client -servername mp3-app.com -connect mp3-app.com:443 2>/dev/null | grep "subject="
echo ""

# 4. فحص Nameservers
echo "4. فحص Nameservers:"
dig mp3-app.com NS +short
echo ""

echo "✅ انتهى الفحص"
```

### تجديد SSL تلقائي

```bash
# Let's Encrypt يجدد تلقائياً قبل انتهاء الصلاحية بـ 30 يوم
# لا تحتاج إلى فعل شيء - Manus يتولى ذلك

# لكن يمكنك فحص صلاحية الشهادة:
echo | openssl s_client -servername mp3-app.com -connect mp3-app.com:443 2>/dev/null | grep -A2 "Validity"
```

---

## 🚨 استكشاف الأخطاء

### المشكلة: النطاق لا يعمل

```bash
# الحل:
1. تحقق من تحديث Nameservers
2. انتظر 24-48 ساعة للانتشار
3. امسح ذاكرة التخزين المؤقت للمتصفح
4. جرب في متصفح آخر
5. استخدم VPN مختلف
```

### المشكلة: SSL Certificate غير صحيح

```bash
# الحل:
1. تأكد من تفعيل HTTPS في Settings
2. أعد إصدار الشهادة
3. امسح ذاكرة التخزين المؤقت للمتصفح
4. انتظر 5 دقائق
```

### المشكلة: البريد الإلكتروني لا يعمل

```bash
# الحل:
1. تحقق من MX Records
2. تحقق من SPF Record
3. أضف DKIM Record
4. اختبر باستخدام: https://mxtoolbox.com/
```

---

## ✅ قائمة التحقق النهائية

- [ ] تم إضافة النطاق إلى Manus Platform
- [ ] تم تحديث Nameservers أو DNS Records
- [ ] تم الانتظار 24-48 ساعة للانتشار
- [ ] تم التحقق من انتشار DNS
- [ ] تم تفعيل SSL Certificate
- [ ] تم التحقق من ملكية النطاق
- [ ] تم تفعيل HTTPS
- [ ] تم اختبار الاتصال في المتصفح
- [ ] تم اختبار الصفحة الرئيسية
- [ ] تم اختبار جميع الصفحات الرئيسية
- [ ] تم اختبار الدوال الأساسية
- [ ] تم اختبار الدفع (إن وجد)

---

## 📞 الدعم والمساعدة

| المشكلة | الحل |
|--------|------|
| DNS لا ينتشر | انتظر 24-48 ساعة |
| SSL غير صحيح | أعد إصدار الشهادة |
| الموقع لا يحمل | تحقق من الاتصال بالخادم |
| البريد لا يعمل | تحقق من MX Records |

---

## 🎉 النتيجة النهائية

بعد إكمال جميع الخطوات:

✅ **النطاق mp3-app.com مرتبط بنجاح**
✅ **SSL Certificate مفعل**
✅ **HTTPS يعمل بشكل صحيح**
✅ **التطبيق متاح على الإنترنت**
✅ **جاهز للاستخدام الفوري**

---

**تم الإعداد بواسطة:** فريق التطوير  
**التاريخ:** 11 فبراير 2026  
**الحالة:** مكتمل وجاهز للنشر
