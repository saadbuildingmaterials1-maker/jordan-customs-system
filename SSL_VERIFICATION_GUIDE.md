# دليل التحقق من SSL والنطاق
# SSL and Domain Verification Guide

**التاريخ**: 11 فبراير 2026  
**التطبيق**: نظام إدارة تكاليف الشحن والجمارك الأردنية  
**الحالة**: جاهز للاختبار

---

## 🔐 معلومات شهادة SSL

### الشهادة الحالية

| المعلومة | القيمة |
|---------|--------|
| **المُصدِّر** | Let's Encrypt |
| **النطاق** | mp3-app.com |
| **نوع الشهادة** | TLS/SSL (HTTPS) |
| **التجديد التلقائي** | مفعّل |
| **صلاحية الشهادة** | 90 يوم |

### معايير الأمان

- ✓ HSTS (HTTP Strict Transport Security)
- ✓ X-Frame-Options: DENY
- ✓ X-Content-Type-Options: nosniff
- ✓ Content-Security-Policy
- ✓ تشفير TLS 1.2+

---

## 🧪 اختبارات التحقق

### الاختبار 1: التحقق من شهادة SSL

#### على Windows (PowerShell):

```powershell
$cert = [System.Net.ServicePointManager]::FindServicePoint("https://mp3-app.com").Certificate
$cert | Select-Object *
```

**النتيجة المتوقعة**:
```
Issuer: CN=R3, O=Let's Encrypt, C=US
Subject: CN=mp3-app.com
ValidFrom: [تاريخ البدء]
ValidUntil: [تاريخ الانتهاء]
```

#### على Mac/Linux:

```bash
openssl s_client -connect mp3-app.com:443 -servername mp3-app.com
```

**النتيجة المتوقعة**:
```
Verify return code: 0 (ok)
Issuer: C = US, O = Let's Encrypt, CN = R3
Subject: CN = mp3-app.com
```

### الاختبار 2: اختبار الاتصال الأساسي

```bash
curl -I https://mp3-app.com
```

**النتيجة المتوقعة**:
```
HTTP/2 200
Content-Type: text/html; charset=utf-8
Strict-Transport-Security: max-age=31536000; includeSubDomains
```

### الاختبار 3: التحقق من رؤوس الأمان

```bash
curl -I -H "User-Agent: Mozilla/5.0" https://mp3-app.com
```

**يجب أن تتضمن**:
- `Strict-Transport-Security`
- `X-Frame-Options`
- `X-Content-Type-Options`
- `Content-Security-Policy`

### الاختبار 4: اختبار الأداء

```bash
curl -w "@curl-format.txt" -o /dev/null -s https://mp3-app.com
```

**المعايير المقبولة**:
- وقت الاتصال: < 200ms
- وقت الاستجابة: < 500ms
- حجم الصفحة: < 2MB

---

## 🔍 أدوات التحقق عبر الإنترنت

### 1. SSL Labs (https://www.ssllabs.com/ssltest/)

1. أدخل: `mp3-app.com`
2. انقر على **Submit**
3. انتظر النتائج (قد تستغرق 2-3 دقائق)

**النتيجة المتوقعة**: A+ أو A

### 2. What's My DNS (https://www.whatsmydns.net/)

1. أدخل: `mp3-app.com`
2. اختر: **CNAME** أو **A**
3. انقر على **Search**

**النتيجة المتوقعة**: جميع الخوادم تظهر نفس القيمة

### 3. Censys (https://censys.io/)

1. اذهب إلى: https://censys.io/certificates/
2. ابحث عن: `mp3-app.com`
3. تحقق من صحة الشهادة

### 4. Qualys SSL Labs

- **الرابط**: https://www.ssllabs.com/ssltest/analyze.html?d=mp3-app.com
- **المدة**: 2-3 دقائق
- **النتيجة**: تقرير شامل عن الأمان

---

## 📊 جدول التحقق الشامل

| الاختبار | الأداة | النتيجة المتوقعة | الحالة |
|---------|-------|-----------------|--------|
| شهادة SSL | openssl | Verify: OK | ⏳ |
| DNS CNAME | whatsmydns.net | متطابق | ⏳ |
| HTTPS | curl | HTTP/2 200 | ⏳ |
| رؤوس الأمان | curl -I | جميع الرؤوس | ⏳ |
| الأداء | curl -w | < 500ms | ⏳ |
| SSL Grade | SSL Labs | A+ | ⏳ |
| التطبيق | المتصفح | يعمل بدون أخطاء | ⏳ |

---

## ⚠️ استكشاف الأخطاء الشائعة

### المشكلة 1: "Certificate not yet valid"

**السبب**: الشهادة لم تُصدر بعد

**الحل**:
1. انتظر 5-10 دقائق
2. امسح ذاكرة المتصفح
3. جرب متصفح مختلف

### المشكلة 2: "Certificate has expired"

**السبب**: الشهادة انتهت صلاحيتها

**الحل**:
1. تحقق من تاريخ انتهاء الشهادة
2. تأكد من تفعيل التجديد التلقائي
3. أعد تشغيل الخادم

### المشكلة 3: "Hostname mismatch"

**السبب**: النطاق لا يطابق الشهادة

**الحل**:
1. تحقق من أن DNS يشير إلى الخادم الصحيح
2. تأكد من أن النطاق في الشهادة صحيح
3. أعد إصدار الشهادة إذا لزم الأمر

### المشكلة 4: "Mixed content warning"

**السبب**: تحميل موارد عبر HTTP بدلاً من HTTPS

**الحل**:
1. افتح وحدة التحكم (F12)
2. ابحث عن تحذيرات Mixed Content
3. غيّر جميع الروابط إلى HTTPS
4. امسح ذاكرة المتصفح

### المشكلة 5: "Connection refused"

**السبب**: الخادم لا يستجيب

**الحل**:
1. تحقق من أن الخادم يعمل
2. تحقق من جدار الحماية
3. جرب النطاق المؤقت
4. أعد تشغيل الخادم

---

## 🔄 عملية التحقق الكاملة

### المرحلة 1: التحضير (قبل الانتشار)

```bash
# 1. تحقق من DNS records
nslookup mp3-app.com

# 2. تحقق من الاتصال الأساسي
ping mp3-app.com

# 3. تحقق من المنفذ 443
telnet mp3-app.com 443
```

### المرحلة 2: التحقق الأولي (بعد الانتشار)

```bash
# 1. اختبر الاتصال الآمن
curl -I https://mp3-app.com

# 2. تحقق من الشهادة
openssl s_client -connect mp3-app.com:443

# 3. اختبر الأداء
curl -w "Time: %{time_total}s\n" https://mp3-app.com
```

### المرحلة 3: التحقق المتقدم

```bash
# 1. اختبر جميع رؤوس الأمان
curl -I https://mp3-app.com | grep -i "security\|cache\|frame"

# 2. اختبر إعادة التوجيه
curl -I http://mp3-app.com

# 3. اختبر www subdomain
curl -I https://www.mp3-app.com
```

### المرحلة 4: اختبار التطبيق

1. **افتح المتصفح**:
   ```
   https://mp3-app.com
   ```

2. **تحقق من الصفحات الرئيسية**:
   - الصفحة الرئيسية
   - صفحة تسجيل الدخول
   - لوحة التحكم

3. **اختبر الميزات الأساسية**:
   - تسجيل الدخول
   - حساب التكاليف
   - تحميل الملفات
   - تصدير التقارير

4. **تحقق من وحدة التحكم**:
   - اضغط F12
   - اذهب إلى **Console**
   - تحقق من عدم وجود أخطاء حمراء

---

## 📈 مؤشرات الأداء

### معايير الأداء المقبولة

| المقياس | القيمة المقبولة | الحالة الحالية |
|---------|-----------------|-----------------|
| **وقت التحميل** | < 3 ثانية | ⏳ |
| **وقت الاستجابة** | < 500ms | ⏳ |
| **حجم الصفحة** | < 2MB | ⏳ |
| **عدد الطلبات** | < 50 | ⏳ |
| **درجة SSL** | A+ | ⏳ |
| **Uptime** | > 99.5% | ⏳ |

---

## 🛡️ معايير الأمان

### التحقق من الأمان

```bash
# 1. اختبر HSTS
curl -I https://mp3-app.com | grep -i "strict-transport"

# 2. اختبر X-Frame-Options
curl -I https://mp3-app.com | grep -i "x-frame"

# 3. اختبر CSP
curl -I https://mp3-app.com | grep -i "content-security"

# 4. اختبر CORS
curl -I https://mp3-app.com | grep -i "access-control"
```

### النتائج المتوقعة

```
Strict-Transport-Security: max-age=31536000
X-Frame-Options: DENY
X-Content-Type-Options: nosniff
Content-Security-Policy: default-src 'self'
```

---

## 📋 قائمة التحقق النهائية

- [ ] شهادة SSL صحيحة وسارية
- [ ] DNS يشير إلى الخادم الصحيح
- [ ] HTTPS يعمل بدون تحذيرات
- [ ] جميع رؤوس الأمان موجودة
- [ ] التطبيق يحمّل بسرعة
- [ ] جميع الصفحات تعمل بدون أخطاء
- [ ] وحدة التحكم خالية من الأخطاء
- [ ] الميزات الأساسية تعمل
- [ ] الأداء مقبول
- [ ] لا توجد تحذيرات أمان

---

## 🔗 الروابط المفيدة

- **SSL Labs**: https://www.ssllabs.com/ssltest/
- **What's My DNS**: https://www.whatsmydns.net/
- **Censys**: https://censys.io/
- **Mozilla Observatory**: https://observatory.mozilla.org/
- **Qualys SSL Labs**: https://www.qualys.com/

---

## 📞 الدعم الفني

إذا واجهت مشاكل:

1. تحقق من ملفات السجل:
   ```bash
   tail -f /home/ubuntu/jordan-customs-system/.manus-logs/devserver.log
   ```

2. تحقق من حالة الخادم:
   ```bash
   ps aux | grep "node\|pnpm"
   ```

3. أعد تشغيل الخادم:
   ```bash
   cd /home/ubuntu/jordan-customs-system
   pnpm run dev
   ```

---

**آخر تحديث**: 11 فبراير 2026  
**الحالة**: جاهز للاختبار  
**النطاق**: mp3-app.com  
**الشهادة**: Let's Encrypt (TLS/SSL)
