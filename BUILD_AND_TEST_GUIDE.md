# 🔨 دليل البناء والاختبار الشامل

## 📋 نظرة عامة

هذا الدليل يشرح كيفية بناء واختبار مثبت Windows بشكل صحيح وفعال 100%.

---

## 🎯 الاستراتيجية المتكاملة

```
┌─────────────────────────────────────────────────────────┐
│          استراتيجية البناء والاختبار المتكاملة         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1️⃣ GitHub Actions (بناء تلقائي على Windows)          │
│     ├─ بناء تلقائي عند كل push/tag                    │
│     ├─ إنشاء Installer و Portable                      │
│     └─ نشر على GitHub Releases                        │
│                                                         │
│  2️⃣ Windows VM محلي (اختبارات رسومية)                 │
│     ├─ تحميل الملف من GitHub                          │
│     ├─ اختبار واجهة التثبيت                           │
│     ├─ التحقق من الاختصارات                           │
│     └─ اختبار التطبيق الكامل                          │
│                                                         │
│  3️⃣ Windows Server سحابي (اختبارات متقدمة)            │
│     ├─ اختبارات على أنظمة مختلفة                      │
│     ├─ اختبارات الأداء                                │
│     └─ اختبارات التوافقية                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 المرحلة 1️⃣: GitHub Actions (البناء التلقائي)

### الإعداد:

```bash
# 1. تأكد من وجود workflow
# المسار: .github/workflows/build-windows-installer.yml

# 2. تأكد من أن المستودع عام (Public)
# GitHub > Settings > Visibility > Public

# 3. تفعيل GitHub Actions
# GitHub > Settings > Actions > Allow all actions
```

### تشغيل البناء:

#### الطريقة 1️⃣: البناء التلقائي (عند Push)

```bash
# 1. اضغط على main branch
git push origin main

# 2. GitHub Actions سيبدأ تلقائياً
# GitHub > Actions > "بناء مثبت Windows الاحترافي"

# 3. انتظر اكتمال البناء (5-10 دقائق)
```

#### الطريقة 2️⃣: البناء اليدوي (Workflow Dispatch)

```bash
# 1. اذهب إلى GitHub
# https://github.com/saadbuildingmaterials1-maker/jordan-customs-system

# 2. اختر Actions
# 3. اختر "بناء مثبت Windows الاحترافي"
# 4. انقر "Run workflow"
# 5. انقر "Run workflow" مرة أخرى
```

#### الطريقة 3️⃣: البناء عند الإصدار (Tag)

```bash
# 1. أنشئ tag جديد
git tag v1.0.1
git push origin v1.0.1

# 2. GitHub Actions سيبدأ تلقائياً
# 3. سيُنشئ GitHub Release تلقائياً
# 4. سيرفع الملفات على Release
```

### الحصول على الملفات:

```
1. اذهب إلى GitHub Actions
2. اختر أحدث workflow
3. انزل إلى "Artifacts"
4. حمل "windows-installer"
5. استخرج الملف
```

---

## 🖥️ المرحلة 2️⃣: Windows VM محلي (الاختبارات الرسومية)

### الإعداد:

```
1. ثبت VirtualBox أو VMware
2. حمل Windows 10/11 ISO
3. أنشئ VM بـ 4-6 GB RAM
4. ثبت Windows
5. ثبت الأدوات الضرورية (Git, Node.js, pnpm)
```

### الاختبار:

اتبع **WINDOWS_VM_TESTING_GUIDE.md** للخطوات الكاملة.

---

## ☁️ المرحلة 3️⃣: Windows Server سحابي (اختبارات متقدمة)

### الإعداد:

```bash
# استخدم AWS EC2 أو Azure
# 1. أنشئ Windows Server 2022 instance
# 2. اتصل عبر RDP
# 3. ثبت الأدوات الضرورية
# 4. اختبر الملف
```

### الفوائد:

```
✅ اختبار على أنظمة مختلفة
✅ اختبار الأداء
✅ اختبار التوافقية
✅ اختبار الشبكة
```

---

## 🔧 إعدادات البناء المهمة

### electron-builder.yml

```yaml
win:
  target:
    - nsis          # مثبت Windows
    - portable      # نسخة محمولة

nsis:
  oneClick: false   # واجهة تثبيت كاملة
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: نظام إدارة تكاليف الشحن والجمارك
```

### package.json

```json
{
  "scripts": {
    "build": "vite build && esbuild ...",
    "electron:build": "npm run build && electron-builder --publish never"
  }
}
```

---

## 📊 مراحل البناء

### 1️⃣ تثبيت المكتبات

```bash
pnpm install --frozen-lockfile
```

**الوقت**: 2-3 دقائق
**الملفات**: node_modules/ (1.5 GB)

### 2️⃣ بناء Vite

```bash
pnpm run build
```

**الوقت**: 1-2 دقيقة
**الملفات**: dist/ (5-10 MB)

### 3️⃣ بناء Server

```bash
esbuild server/_core/index.ts --platform=node ...
```

**الوقت**: 30 ثانية
**الملفات**: dist/index.js

### 4️⃣ بناء Electron

```bash
electron-builder
```

**الوقت**: 2-3 دقائق
**الملفات**: 
- JordanCustomsSystem-Setup-1.0.0.exe (200 MB)
- JordanCustomsSystem-1.0.0-portable.exe (180 MB)

---

## ✅ قائمة التحقق قبل البناء

- [ ] جميع الملفات محفوظة
- [ ] لا توجد أخطاء TypeScript
- [ ] جميع الاختبارات تمر
- [ ] الكود نظيف (no console.log)
- [ ] الإصدار محدث في package.json
- [ ] Git commits محفوظة

---

## 🐛 استكشاف مشاكل البناء

### المشكلة: فشل تثبيت المكتبات

```bash
# الحل:
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

### المشكلة: أخطاء TypeScript

```bash
# الحل:
pnpm run check
# إصلاح الأخطاء
```

### المشكلة: فشل بناء Electron

```bash
# الحل:
rm -rf dist_electron
pnpm run electron:build --verbose
```

### المشكلة: ملف EXE كبير جداً

```bash
# التحقق من حجم الملف:
ls -lh dist_electron/*.exe

# يجب أن يكون حوالي 200 MB
# إذا كان أكبر، تحقق من node_modules
```

---

## 📈 مؤشرات النجاح

### بناء ناجح:

```
✅ لا توجد أخطاء في البناء
✅ ملفات EXE موجودة
✅ حجم الملف معقول (150-250 MB)
✅ الملف يمكن تشغيله على Windows
```

### اختبار ناجح:

```
✅ واجهة التثبيت تظهر فوراً
✅ التثبيت يكتمل بنجاح
✅ الاختصارات تُنشأ تلقائياً
✅ التطبيق يبدأ بسرعة
✅ لا توجد أخطاء في التطبيق
```

---

## 📝 نموذج تقرير البناء

```markdown
# تقرير البناء

## معلومات البناء
- **التاريخ**: [التاريخ]
- **الإصدار**: [رقم الإصدار]
- **المنصة**: Windows
- **المُنشئ**: GitHub Actions / محلي

## نتائج البناء
- [ ] تثبيت المكتبات: ✅ نجح
- [ ] بناء Vite: ✅ نجح
- [ ] بناء Server: ✅ نجح
- [ ] بناء Electron: ✅ نجح

## الملفات المُنتجة
- [ ] JordanCustomsSystem-Setup-*.exe (200 MB)
- [ ] JordanCustomsSystem-*-portable.exe (180 MB)

## الاختبارات
- [ ] واجهة التثبيت: ✅ تعمل
- [ ] التثبيت الكامل: ✅ ناجح
- [ ] الاختصارات: ✅ موجودة
- [ ] تشغيل التطبيق: ✅ يعمل

## الملاحظات
[أضف أي ملاحظات هنا]

## الحالة النهائية
- [ ] ✅ جاهز للإصدار
- [ ] ⚠️ يحتاج إصلاحات
- [ ] ❌ فشل
```

---

## 🔗 الروابط المهمة

- **GitHub Repository**: https://github.com/saadbuildingmaterials1-maker/jordan-customs-system
- **GitHub Actions**: https://github.com/saadbuildingmaterials1-maker/jordan-customs-system/actions
- **GitHub Releases**: https://github.com/saadbuildingmaterials1-maker/jordan-customs-system/releases
- **VirtualBox**: https://www.virtualbox.org/
- **Windows 10 ISO**: https://www.microsoft.com/en-us/software-download/windows10
- **Windows 11 ISO**: https://www.microsoft.com/en-us/software-download/windows11

---

## 📞 الدعم

إذا واجهت مشاكل:

1. **تحقق من الأدلة أعلاه**
2. **تحقق من GitHub Issues**
3. **اتصل بالدعم**:
   - 📧 support@manus.im
   - 📱 +962 795 917 424

---

**✅ تم إعداد البناء والاختبار بنجاح! 🎉**
